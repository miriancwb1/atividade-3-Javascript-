function verificarArrayVazio(array) {
    if (array.length === 0) {
        console.log("O array está vazio.");
    } else {
        console.log("O array não está vazio.");
    }
}

// Exemplo de uso
const numeros = [];
verificarArrayVazio(numeros);
